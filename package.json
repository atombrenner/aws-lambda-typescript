{
  "name": "aws-lambda-typescript",
  "version": "1.0.0",
  "description": "An AWS Lambda function in Typescript",
  "license": "GPL-3.0-only",
  "repository": "github:atombrenner/aws-lambda-typescript",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf ./dist",
    "build": "parcel build src/index.ts",
    "zip": "cd dist && zip -q -r lambda.zip . && ls -lh lambda.zip",
    "predeploy": "npm t && npm run build && npm run zip",
    "deploy": "aws lambda update-function-code --function-name aws-lambda-typescript --zip-file fileb://dist/lambda.zip",
    "stack": "ts-node scripts/stack.ts"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test"
    }
  },
  "dependencies": {
    "aws-sdk": "^2.411.0",
    "zlib": "^1.0.5"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "@babel/preset-typescript": "^7.10.1",
    "@types/aws-lambda": "^8.10.19",
    "@types/jest": "^26.0.0",
    "@types/node": "^12.12.47",
    "babel-jest": "^26.0.1",
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.0.5",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
